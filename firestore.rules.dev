rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Open rules for development/testing
    // ⚠️ WARNING: Replace with secure rules before production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
